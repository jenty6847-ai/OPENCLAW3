# 2026-02-13

- **Task:** Enable LAN access for OpenClaw on desktop host.
- **Action:** Updated `openclaw.json`: set `gateway.mode` to `"remote"` and `gateway.bind` to `"lan"` to allow external connections.
- **Blocker:** Cannot add Windows Firewall rule automatically (requires Admin/Elevated privileges).
- **Instruction:** User must manually run `netsh advfirewall firewall add rule name="OpenClaw Gateway" dir=in action=allow protocol=TCP localport=18789` as Administrator.
- **Next Step:** User needs to verify access from laptop using the desktop's LAN IP (e.g., `http://192.168.x.x:18789`).
